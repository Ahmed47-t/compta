# سجل المخاطر وتدابير التخفيف (RISK_REGISTER.md)

**المشروع:** PC COMPTA Next (`Ahmed47-t/compta`)  
**الإصدار:** 1.0 — يوليو 2026  
**الهدف:** التحديد المنهجي للمخاطر المعمارية، والمحاسبية، والأمنية، والتشغيلية المحتملة، مع تقييم احتمالية حدوثها وتأثيرها، وتحديد الإجراءات المضادة الدقيقة لتخفيفها (`Mitigation Strategies`).

---

## 1. المخاطر المحاسبية والقانونية (Domain & Legal Risks)

| معرف الخطر (`Risk ID`) | الوصف وتفاصيل الخطر (`Description`) | الاحتمالية (`Probability`) | التأثير (`Impact`) | خطة التخفيف والإجراءات المضادة (`Mitigation Strategy`) | المالِك (`Owner`) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **RSK-DOM-01** | **تغير أو عدم دقة النماذج الضريبية (G50 / Liasse Fiscale):** صدور تعديلات قانونية جديدة من مديرية الضرائب الجزائرية (`DGI`) غير مضمنة في النماذج الافتراضية، أو اختلاف تفسير الصيغ الجبائية. | متوسطة (`Medium`) | مرتفع جداً (`High`) | 1. فصل تعريفات التقارير الجبائية عن النواة البرمجية عبر محرك مترجم الصيغ (`Dictionnaire Compiler`) القابل للتعديل دون إعادة بناء التطبيق.<br>2. اعتماد مصفوفة المتطلبات الجبائية من طرف خبير محاسبي جزائري معتمد قبل إطلاق المرحلة 14.<br>3. إضافة تنبيه للمستخدم بحساب النسخة والتاريخ الصادر به النموذج الضريبي. | `Tax Architect + Domain Expert` |
| **RSK-DOM-02** | **انتهاك قيد التوازن المحاسبي في حالات استثنائية:** حدوث خلل تقني أو انقطاع طاقة أثناء حفظ قيد معقد يؤدي لتسجيل أسطر مدينة دون دائنة. | منخفضة (`Low`) | حرج (`Critical`) | 1. فرض المعاملات الذرية (`ACID Database Transactions`) على مستوى قاعدة البيانات (`SQLAlchemy Session + PostgreSQL Sync`).<br>2. تفعيل قيد ميكانيكي إلزامي (`CHECK Trigger`) في قاعدة البيانات يمنع ترحيل أي فوليو لا يتحقق فيه `SUM(Débit) == SUM(Crédit)`. | `Principal PostgreSQL Engineer` |
| **RSK-DOM-03** | **أخطاء التقريب الرياضي للمبالغ:** تراكم أخطاء أجزاء السنتيم في العمليات الحسابية والتقارير الكبيرة بسبب التحويلات الخاطئة بين اللغات. | منخفضة (`Low`) | مرتفع (`High`) | الحظر التام لاستخدام الفواصل العائمة (`float/double`) واعتماد النوع `NUMERIC(19,2)` في DB، و `Decimal` في Python/Rust، واختبارات التقريب الصارمة (`Property-based Rounding Tests`). | `Core Engine Lead` |

---

## 2. المخاطر المعمارية والأداء (Architectural & Performance Risks)

| معرف الخطر (`Risk ID`) | الوصف وتفاصيل الخطر (`Description`) | الاحتمالية (`Probability`) | التأثير (`Impact`) | خطة التخفيف والإجراءات المضادة (`Mitigation Strategy`) | المالِك (`Owner`) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **RSK-ARC-01** | **بطء الاستجابة والبحث في الجداول الضخمة (> 500k قيد):** تراجع سرعة فتح ميزان المراجعة أو بطء التمرير في شبكة الفوليو على أجهزة الحواسيب الضعيفة (i3 / 4GB RAM) المنتشرة في المكاتب. | متوسطة (`Medium`) | مرتفع (`High`) | 1. استخدام معمارية خفيفة جداً (Tauri Rust + React Virtualized Grid) تستهلك أقل من 150MB RAM.<br>2. تقسيم الجداول زمنياً (`PostgreSQL Partitioning by Exercice`).<br>3. الفهرسة الدلالية السريعة باستخدام `pg_trgm` و `btree_gin`، واعتماد Materialized Views لأرصدة ميزان المراجعة. | `Desktop & DB Architect` |
| **RSK-ARC-02** | **تعقيد تثبيت وصيانة PostgreSQL محلياً على Windows:** مواجهة صعوبات من قبل المحاسب غير التقني في تثبيت خدمة PostgreSQL أو إدارة ترقياتها أو النسخ الاحتياطي على جهاز مكتبي منفرد. | عالية (`High`) | متوسط (`Medium`) | 1. بناء حزمة تثبيت مدمجة (`Windows Embedded Installer via NSIS`) تقوم بتثبيت وضبط وبدء خدمة PostgreSQL محلياً بضغطة زر واحدة.<br>2. توفير واجهة إدارة سهلة ومدمجة في سطح المكتب للنسخ الاحتياطي والاستعادة الدورية (`One-Click Backup/Restore UI`). | `Staff Platform Engineer` |

---

## 3. المخاطر الأمنية وحماية البيانات (Security & Anti-Tamper Risks)

| معرف الخطر (`Risk ID`) | الوصف وتفاصيل الخطر (`Description`) | الاحتمالية (`Probability`) | التأثير (`Impact`) | خطة التخفيف والإجراءات المضادة (`Mitigation Strategy`) | المالِك (`Owner`) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **RSK-SEC-01** | **التلاعب المادي بقاعدة البيانات من خارج التطبيق:** دخول مستخدم محلي إلى ملفات قاعدة البيانات أو أداة `pgAdmin` ومحاولة تعديل أرقام القيود المعتمدة أو حذف سجلات التدقيق لتغطية اختلاس. | متوسطة (`Medium`) | حرج (`Critical`) | 1. تطبيق نظام التحكم بالوصول وتقييد الصفوف (`Row-Level Security RLS`) ورفض أوامر التعديل المباشر.<br>2. تشفير كافة البيانات المخزنة (`AES-256 at rest`) وحفظ مفاتيح التشفير بأمان في `Windows Keychain`.<br>3. ربط القيود بسلسلة تجزئة تشفيرية (`Hash Chain`) وتوقيع الأختام بـ `RSA Signature`؛ أي تعديل خارجي سيكسره النظام ويكشف التزوير فوراً. | `Application Security Engineer` |
| **RSK-SEC-02** | **فقدان وتلف مفاتيح التراخيص (`License Bypass / Dongle issues`):** تعطل التراخيص الرقمية عند تغيير عتاد الحاسوب أو انقطاع الإنترنت. | متوسطة (`Medium`) | متوسط (`Medium`) | استبدال نظام الـ Dongle المعرض للتلف بمفاتيح ترخيص رقمية موقعة بـ `RSA` ترتبط بالبصمة العتادية (`Hardware Fingerprint`) مع سياسة عمل غير متصلة (`Graceful Offline Policy`) تتيح للمكتب الاستمرار بالعمل دون تعطل في حال تغيير قطعة عتادية بسيطة. | `Security Architect` |

---

## 4. مخاطر محرك الـ OCR والهجرة (OCR & Migration Risks)

| معرف الخطر (`Risk ID`) | الوصف وتفاصيل الخطر (`Description`) | الاحتمالية (`Probability`) | التأثير (`Impact`) | خطة التخفيف والإجراءات المضادة (`Mitigation Strategy`) | المالِك (`Owner`) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **RSK-OCR-01** | **انخفاض دقة استخراج الـ OCR محلياً على الفواتير المشوهة:** فشل قراءة المبالغ أو الأرقام الجبائية في الفواتير الجزائرية الرديئة الطباعة أو المكتوبة بخط اليد جزئياً. | عالية (`High`) | متوسط (`Medium`) | 1. تطبيق معالجة قبلية متقدمة للصورة (`Deskew, Denoise, Contrast enhancement`).<br>2. دمج خوارزميات التحقق الرياضي التلقائي (`MONTANT_TTC == HT + TVA`).<br>3. **الحظر التام للاعتماد التلقائي (`No Auto-Posting`):** كل وثيقة تمر عبر واجهة تحقق (`Validation UI`) تبرز نسبة الثقة (`Confidence Score`) وتتطلب تأكيد المحاسب البشري. | `Document AI Engineer` |
| **RSK-MIG-01** | **تلف الملفات القديمة (.DLG/.PCC) واختلاف ترميز الحروف:** فشل هجرة قواعد البيانات القديمة بسبب تلف الحقول أو اختلاف ترميز اللغة العربية (`CP1256 / Windows-1256 vs UTF-8`). | عالية (`High`) | مرتفع (`High`) | 1. بناء محول ذكي للترميز (`Encoding Profiler & Sanitizer`) يفحص الملفات ويصلح الترميز قبل الإدراج.<br>2. تشغيل الهجرة في بيئة تجريبية مع إصدار تقرير الفروقات والمطابقة (`Reconciliation Audit Report`) والتأكد من أن `فرق الرصيد = 0.00 دج` قبل الاعتماد النهائي. | `Data Migration Architect` |
