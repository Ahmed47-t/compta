# الأسئلة المفتوحة والقرارات غير المحسومة (OPEN_QUESTIONS.md)

**المشروع:** PC COMPTA Next (`Ahmed47-t/compta`)  
**تاريخ آخر تحديث:** 2026-07-16  
**الهدف:** توثيق جميع النقاط الفنية، والقانونية، والجبائية، والتشغيلية التي لا تزال تحتاج إلى استيضاح، أو عينات اختبار مادية، أو اعتماد رسمي من خبير بشري (محاسبي/جبائي/قانوني).

---

## 1. أسئلة حول بيئة المستخدم المستهدف والأجهزة الدنيا (`Target User & Minimum Hardware`)

| معرف السؤال (`Q-ID`) | السؤال والتفاصيل (`Question & Context`) | التأثير المعماري (`Impact`) | صاحب القرار / المسؤول عن الإجابة | التوصية والموقف الفني الحالي |
| :--- | :--- | :--- | :--- | :--- |
| **Q-HW-01** | **ما هي النسبة الدقيقة لتوزيع أنظمة التشغيل وحجم الذاكرة في مكاتب الخبراء المحاسبيين بالجزائر حالياً؟** (هل نعتبر `4GB RAM Core i3 4th Gen Windows 10 64-bit` هو الحد الأدنى الإلزامي، أم هناك مكاتب لا تزال تعمل على Windows 7 أو 32-bit؟) | تحديد أهداف بناء حزمة Tauri وتثبيت خدمة PostgreSQL 16 ومحرك ONNX محلياً. | `Product Manager + Survey Data` | **الموقف الحالي:** بناء وتوجيه التطبيق للعمل على `Windows 10/11 64-bit` مع حد أدنى `4GB RAM` (وموصى به `8GB RAM`). لن يتم دعم بيئات 32-bit أو Windows 7 لعدم التوافق الأمني والتقني مع PostgreSQL 16 و Rust الحديث. |
| **Q-HW-02** | **هل تحتاج المكاتب لتشغيل النظام في شبكة محلية (`LAN Client-Server`) من اليوم الأول في V1، أم نركز على التثبيت المنفرد (`Single-Node Desktop`) أولاً؟** | هندسة التثبيت وإعدادات جدار الحماية ومنافذ `FastAPI/PostgreSQL`. | `Product Owner / Client` | **الموقف الحالي:** V1 يُصمم كـ `Modular Monolith Desktop` مع قدرة قاعدة بيانات PostgreSQL على استقبال اتصالات من حواسيب أخرى في الشبكة المحلية عبر إعدادات `pg_hba.conf` والمصادقة المعتمدة. |

---

## 2. أسئلة حول ملفات وقواعد بيانات PC COMPTA الأصلي (`DLG & PCC Formats`)

| معرف السؤال (`Q-ID`) | السؤال والتفاصيل (`Question & Context`) | التأثير المعماري (`Impact`) | صاحب القرار / المسؤول عن الإجابة | التوصية والموقف الفني الحالي |
| :--- | :--- | :--- | :--- | :--- |
| **Q-DLG-01** | **ما هي ترميزات الحروف (`Character Encodings`) الفعلية المستخدمة في أسماء الحسابات والليبل (`LIBELLE`) في ملفات `.DLG` المنتشرة في المكاتب؟** (هل هي `Windows-1256 CP1256` أم ترميزات DOS قديمة مثل `CP864`؟) | بناء أداة تحويل الترميز في `DLG Reader` ومنع تشوه الحروف العربية. | `Data Migration Lead + Sample Files` | **الموقف الحالي:** سيتم بناء `Parser` في المرحلة 08 والـ `PoC 6` في المرحلة 01 يدعم الاكتشاف التلقائي للترميز (`CP1256 / CP864 / UTF-8`) واختباره على العينات المتاحة للقراءة فقط. |
| **Q-DLG-02** | **هل توجد اختلافات بين إصدارات PC COMPTA المختلفة في بنية حقول جدول اليومية (`PCC_GLV.DLG`)؟** (مثلاً ظهور حقول إضافية أو اختلاف أطوال حقل `REFERENCE` أو `LIBELLE` عبر العقود الماضية). | استقرار أداة الهجرة وعدم تحطمها عند مواجهة حقل جديد أو مفقود. | `Domain Expert / File Samples` | **الموقف الحالي:** تصميم `Migration Engine` بمرونة (`Defensive Parsing`) تتجاهل الحقول غير المعروفة وتوثقها في تقرير استثناءات الهجرة بدل التوقف الفوري. |

---

## 3. أسئلة حول النماذج والتقارير الجبائية الرسمية (`DGI Tax Reports & Liasse`)

| معرف السؤال (`Q-ID`) | السؤال والتفاصيل (`Question & Context`) | التأثير المعماري (`Impact`) | صاحب القرار / المسؤول عن الإجابة | التوصية والموقف الفني الحالي |
| :--- | :--- | :--- | :--- | :--- |
| **Q-TAX-01** | **ما هي النسخ المعتمدة والأحدث من القوالب الرسمية لتصريح `G50` وحزمة الجداول الثمانية للميزانية (`Liasse Fiscale SCF 8 Tableaux`)؟** | الدقة الجبائية وتجنب رفض التصاريح من طرف مفتشية الضرائب (`DGI`). | `Certified Tax Accountant` | **الموقف الحالي:** عدم برمجة أي صيغة جبائية في الكود الثابت، بل اعتماد مترجم الصيغ (`Dictionnaire Compiler`) واشتراط إيداع النماذج الرسمية وتوقيع مصفوفة المتطلبات الجبائية من خبير قبل إطلاق المرحلة 14. |
| **Q-TAX-02** | **هل هناك أي بوابة إلكترونية رسمية متاحة بـ API وثائق ومواصفات مفتوحة من طرف مديرية الضرائب الجزائرية (`DGI Jibayatic / Moussahamatic`) للرفع المباشر؟** | تحديد ما إذا كان النظام سيدعم التصدير الإلكتروني أم يكتفي بتوليد PDF/Excel. | `Legal & Tax Advisor` | **الموقف الحالي:** افتراض عدم وجود API مفتوح للربط المباشر؛ النظام سيولد نماذج PDF جاهزة للطباعة ومطابقة بنسبة 100%، بالإضافة لملفات Excel/XML القياسية المساعدة. |

---

## 4. أسئلة وحوكمة سياسة الـ Cloud OCR والتعلم النشط (`Cloud OCR & Active Learning Policy`)

| معرف السؤال (`Q-ID`) | السؤال والتفاصيل (`Question & Context`) | التأثير المعماري (`Impact`) | صاحب القرار / المسؤول عن الإجابة | التوصية والموقف الفني الحالي |
| :--- | :--- | :--- | :--- | :--- |
| **Q-OCR-01** | **ما هي الضوابط القانونية وشروط الخصوصية (`Data Privacy & DPA`) للسماح بإرسال صور فواتير العملاء إلى مزود سحابي أو جمعها لتدريب نموذج `Active Learning`؟** | تصميم شاشات أخذ الموافقة (`Consent UI`) وحوكمة حجب البيانات الحساسة (`Redaction`). | `Legal Privacy Counsel` | **الموقف الحالي:** الـ OCR يعمل في الإصدار V1 محلياً بنسبة 100% وبدون اتصال بالإنترنت. ميزة التعلم النشط والربط السحابي الاختياري مؤجلة بالكامل حتى المرحلة 13 ولن تُفعل إلا بموافقة قانونية مكتوبة. |
