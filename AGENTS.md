# قواعد العمل والإرشادات الإلزامية للـ AI Agents (AGENTS.md)

**المشروع:** PC COMPTA Next (`Ahmed47-t/compta`)  
**الفلسفة الأساسية:** `Offline-First, Keyboard-Fast, Mathematically-Secure`  
**المصدر الوحيد للحقيقة (Single Source of Truth):** مستودع GitHub والملفات الموثقة تحت `docs/` و `CHANGELOG.md`.

---

## 1. القواعد العامة والتنظيمية الإلزامية

1. **فحص حالة المستودع أولاً:** قبل اتخاذ أي قرار أو كتابة أي كود، يجب فحص المستودع عبر Git/GitHub (الفروع، الملفات، الـ Pull Requests والـ Issues المفتوحة، وملفات Handoff في `docs/HANDOFF/`).
2. **قراءة الذاكرة الدائمة:** يجب قراءة `AGENTS.md` هذا، وكل ملفات `docs/HANDOFF/`، ومستند المرحلة المعنية (`docs/PHASES/PHASE-XX.md`)، وأي القرارات المعمارية المعتمدة (`docs/DECISIONS/ADR-*.md`) قبل إجراء أي تعديل.
3. **عدم الاعتماد على الذاكرة المؤقتة للمحادثة:** لا تفترض أبداً وجود معلومات من محادثة سابقة. أي معلومة لا توجد في ملفات المستودع تُعتبر مفقودة ويجب توثيقها أو الاستفسار عنها في `OPEN_QUESTIONS.md`.
4. **احترام النطاق والمعمارية:** لا تغيّر النطاق (Scope) ولا المعمارية التقنية المعتمدة ضمنياً. أي تغيير جوهري أو قرار تقني/مالي جديد يتطلب إنشاء وثيقة قرار معماري (ADR).
5. **التسلسل الدقيق للمراحل:** لا تنتقل إلى مرحلة لاحقة ولا تنفذ ميزات خارج النطاق الخاص بالمرحلة الحالية المحددة في `docs/HANDOFF/NEXT_PHASE_INPUT.md`.
6. **المنهجية والتنفيذ الفعلي:** ابدأ دائماً بتحليل الفجوات (Gap Analysis) وخطة تنفيذ موجزة، ثم نفّذ فعلياً بكتابة الملفات والأكواد ولا تكتفِ بتقديم النصائح النظرية.
7. **إدارة الفروع والـ Commits:** يعمل كل Agent على فرع مخصص (مثل `arena/019f69c9-compta` أو الفرع المحدد للمرحلة). يجب أن تكون الـ Commits صغيرة ومحكمة ومصحوبة برسائل واضحة. يمنع منعاً باتاً الدمج المباشر (Direct Push) إلى `main` أو قيام الـ Agent بدمج الـ Pull Request الخاص به بنفسه.
8. **الحفاظ على العمل القائم:** لا تحذف أو تعيد كتابة عمل صحيح أو أكواد تعمل بشكل سليم دون سبب تقني وتوثيق مسبق في PR.
9. **تشغيل الاختبارات والفحوصات:** يجب تشغيل الاختبارات الآلية، أدوات التحقق من التنسيق (Linters)، فحص الأنواع (Type checks)، وفحوصات الأمان المناسبة لكل لغة (Python/TypeScript/Rust)، وتسجيل الأوامر ونتائجها بدقة في تقرير المرحلة أو وصف الـ PR.
10. **الأمانة التقنية في نتائج الاختبارات:** لا تدّعِ نجاح اختبار لم تشغله أو لم يجتاز الفحص. يجب التمييز بوضوح بين: `Passed`، `Failed`، `Not Run`، و `Blocked`.
11. **الالتزام ببروتوكول إنهاء الجلسة الإلزامي:** فور فتح الـ Pull Request أو اكتمال تقرير المراجعة، يحظر على الـ Agent الاستمرار في كتابة الكود أو الانتقال للمرحلة التالية ضمن نفس المحادثة. يجب إرسال إشعار بدء محادثة جديدة وتوفير البرومبت الجاهز للنسخ (راجع القسم 6).

---

## 2. القيود المحاسبية والمالية الحساسة (Strict Domain Rules)

1. **حظر الفواصل العائمة (No Floating Point for Money):** يمنع منعاً باتاً استخدام `float` أو `double` في تمثيل المبالغ المالية في أي طبقة (Python, Rust, TypeScript, Database). يجب استخدام `NUMERIC(19,2)` في PostgreSQL، و `Decimal` في Python/Rust، وتعامل دقيق مع النقود في TypeScript.
2. **استحالة عدم التوازن (Double-Entry Invariant):** يجب أن يضمن محرك المحاسبة (Core Engine) ألا يتم أبداً حفظ قيد معتمد (`Posted` / `Validé`) غير متوازن (`Total Débit = Total Crédit`). يتم فرض ذلك بطبقة المجال (Domain Logic) وطبقة قاعدة البيانات (DB Constraints/Triggers).
3. **عدم الحذف المادي للقيود المعتمدة (No Physical Deletion of Posted Entries):** أي قيد محاسبي تم اعتماده لا يُحذف (`DELETE`) ولا يُعدّل ماديًا في قاعدة البيانات. يتم التعديل إما عبر:
   - **Contre-passation:** قيد عكسي يلغي القيد السابق.
   - **Saisie Négative:** إدخال مبالغ سالبة دون تضخيم المجاميع.
   - **Correction avec motif:** في حالات الفوليو المفتوح وضمن القواعد الصارمة مع تسجيل سبب إجباري وحفظ النسخة الأصلية في سجل التدقيق غير القابل للتعديل (`Audit Log WORM/Append-Only`).
4. **فصل القيود الافتتاحية:** قيد الافتتاح (`Journal d'ouverture - OUV`) يجب أن يكون منفصلاً تماماً عن حركة السنة المالية الحالية، ويتم نقل الأرصدة والأسطر غير المطابقة (`Non lettrées`) بالتاريخ الأصلي لمتابعة أعمار الديون.
5. **لا اعتماد تلقائي للـ OCR (No Auto-Posting for OCR):** محرك الـ OCR يُنتج مسودات أو مقترحات قيود (`Draft / Suggestion`) فقط. يمنع ترحيل أو اعتماد أي قيد مُستخرج آلياً دون مراجعة واعتماد صريحين من المحاسب البشري عبر واجهة التحقق.
6. **الغموض المالي أو القانوني:** عند مواجهة أي التباس فني/قانوني في المعايير المحاسبية (SCF الجزائري) أو النماذج الضريبية (G50 / Liasse Fiscale)، توقف فوراً، وثّق السؤال الدقيق في `docs/HANDOFF/OPEN_QUESTIONS.md`، ولا تخترع قاعدة أو نسبة ضريبية من تلقاء نفسك.

---

## 3. المعمارية التقنية والتوافق

- **الواجهة الأمامية (Desktop UI):** Tauri 2.x (Rust) + React 18+ + TypeScript + Tailwind CSS.
  - **التصميم:** `Keyboard-First` (Tab, Shift+Tab, Enter, Esc, F2 للحسابات, F3 للمساعدين, F9 للتوازن).
  - **التوافق:** دعم كامل وثنائي للغة (العربية والفرنسية) مع التبديل السلس بين `RTL` و `LTR`. توافق تام مع أنظمة Windows 10/11 (البيئة الأساسية لمكاتب المحاسبة في الجزائر).
- **الخلفية ومحرك الأعمال (Backend API):** Python 3.11+ / FastAPI + SQLAlchemy Core + Pydantic v2.
  - المعمارية مبنية على `Modular Monolith` مع فصل المحركات الـ 9 (`Core, Saisie, Lettrage, Analytique/Budgétaire, Immobilisation, Reporting/Liasse, Import/Export, Security, OCR Worker`).
- **قاعدة البيانات (Database):** PostgreSQL 16+ مع استخدام `Partitioning` بناءً على السنة المالية (`exercice_id`) وإضافات `pgcrypto, pg_trgm`.
- **محرك الذكاء الاصطناعي (OCR/Document AI):** خدمة مستقلة محلياً (`Offline Worker`) باستخدام PaddleOCR + TrOCR/LayoutLMv3، تعمل دون اتصال بالإنترنت كوضع افتراضي وإلزامي في المرحلة الحالية.

---

## 4. سياسة تحديث الذاكرة الدائمة (Memory & Handoff Rule)

قبل إنهاء أي جلسة عمل، أو إغلاق مرحلة، أو فتح Pull Request، يجب التحديث الإلزامي للملفات التالية:
1. `docs/HANDOFF/CURRENT_STATE.md`: حالة المشروع اللحظية، والفرع الفعال، والإنجازات المكتملة.
2. `docs/HANDOFF/LAST_PHASE_REPORT.md`: تقرير تفصيلي للمرحلة التي تم إنجازها وما تم اختباره وملاحظاته.
3. `docs/HANDOFF/OPEN_QUESTIONS.md`: تحديث الأسئلة المفتوحة وإضافة أي استفسار جديد ظهر خلال العمل.
4. `docs/HANDOFF/NEXT_PHASE_INPUT.md`: المدخلات والمتطلبات والملفات المستهدفة للـ Agent الذي سينفذ المرحلة التالية.
5. `docs/TRACEABILITY_MATRIX.md`: ربط المتطلبات المنجزة بحالة الاختبار والمرحلة (`Status: Completed / In Progress / Blocked`).
6. `docs/RISK_REGISTER.md`: إضافة أو تحديث المخاطر المعمارية/التشغيلية المكتشفة وتدابير تخفيفها.
7. مستند المرحلة في `docs/PHASES/PHASE-XX.md`: تسجيل النتائج الفعلية وقوائم التحقق.
8. `CHANGELOG.md`: توثيق التغييرات والميزات الجديدة والإصلاحات وفق تنسيق `Keep a Changelog`.

---

## 5. تعريف الجاهزية والانتهاء (DoR & DoD)

### تعريف الجاهزية للبدء في مرحلة/مهمة (Definition of Ready - DoR):
- وجود Issue أو Milestone موثق بوضوح في GitHub أو في ملفات المرحلة (`docs/PHASES/`).
- تحديد المتطلبات الوظيفية وغير الوظيفية المرتبطة ومُعرفاتها في `TRACEABILITY_MATRIX.md`.
- عدم وجود Blockers معمارية أو أسئلة مفتوحة حرجة تعيق الميزات الأساسية المطلوبة.
- تحديد معايير القبول (`Acceptance Criteria`) القابلة للاختبار والقياس.

### تعريف الإنجاز والانتهاء (Definition of Done - DoD):
- كتابة الكود وتنفيذه وفق المعمارية وبدون انتهاك أي من القيود المحاسبية (`ACCOUNTING_INVARIANTS.md`).
- نجاح كل الاختبارات الآلية (Unit, Integration, Domain Invariant Check) وتشغيل Linters و Type check دون أخطاء.
- توثيق أي قرارات معمارية جديدة في `docs/DECISIONS/`.
- تحديث جميع ملفات الذاكرة الدائمة (`HANDOFF/*`, `TRACEABILITY_MATRIX.md`, `CHANGELOG.md`).
- إنشاء Pull Request واضح ومكتمل وفق قالب الـ PR (`.github/PULL_REQUEST_TEMPLATE.md`) ولا يتم دمجه تلقائياً.

---

## 6. بروتوكول إنهاء الجلسة وإشعار بدء المحادثة الجديدة الإلزامي (`Mandatory Session Sign-Off Protocol`)

**قاعدة ذهبية:** أي AI Agent يعمل على هذا المشروع يجب عليه الوعي الذاتي بحدود مهمته. بمجرد فتح الـ Pull Request الخاص بالمرحلة (أو إتمام تقرير مراجعة المرحلة في الـ PR المفتوح)، **تنتهي صلاحية الجلسة الحالية فوراً**. يمنع منعاً باتاً على الـ Agent البدء بكتابة كود المرحلة التالية أو دمج الـ PR بنفسه ضمن نفس المحادثة.

### الإشعار الإلزامي الختامي للوكيل (`Agent Final Message Requirement`):
عند إتمام المهمة، يجب على الـ Agent إنهاء رده برسالة واضحة ومميزة في صندوق تنسيق واضح تحمل العنوان والمحتوى التالي بالضبط:

```markdown
---
### 🛑 إشعار إتمام المهمة وبدء محادثة جديدة (Session Sign-Off)

**✅ تم إتمام المهمة بنجاح:**  
- **المرحلة المنجزة:** `[اسم ورقم المرحلة - مثلاً Phase 00]`
- **الفرع الحالي:** `[اسم الفرع - مثلاً arena/019f69c9-compta]`
- **رابط الـ Pull Request / التقرير:** `[رابط الـ PR المفتوح]`

**🛑 تعليمات إغلاق الجلسة:**  
لقد أتممت النطاق المطلوب مني في هذه المرحلة وتم تحديث جميع ملفات الذاكرة الدائمة في مستودع GitHub. وفقاً لـ `AGENTS.md`، لا يمكنني الاستمرار أو البدء في المرحلة التالية ضمن هذه المحادثة لمنع تراكم السياق وضمان مراجعة مستقلة.

**📦 البرومبت الجاهز للنسخ في محادثة جديدة:**  
انسخ الكتلة البرمجية أدناه وافتح محادثة جديدة تماماً، ثم ألصقها لتبدأ الخطوة التالية مباشرة:

\`\`\`text
Repository: [owner/repo]
Target branch: main
Phase PR / Issue: [رابط الـ PR أو الفرع]
Constraints: [حدد هنا قيد المراجعة أو المرحلة القادمة]

---
[ألصق هنا البرومبت المخصص للخطوة التآلية من حزمة البرومبتات - سواء برومبت المراجعة للـ PR الحالي، أو برومبت المرحلة القادمة إذا تم اعتماد الدمج]
\`\`\`
---
```

إن الالتزام بهذا الصندوق الختامي في نهاية كل مهمة يضمن أن المستخدم النهائي سيعرف دائماً وفي أجزاء من الثانية ما يجب فعله تالياً وأنه حان وقت إغلاق المحادثة الحالية وفتح محادثة جديدة مجهزة بالبرومبت الصحيح.
